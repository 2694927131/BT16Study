# P0_架构与基础 — Deepseek 任务拆解

> 每个任务是一个独立的 Prompt，可直接复制到 Deepseek 中使用
> 执行顺序：T01 → T02 → ... → T07（有前后依赖关系）
> 每个任务完成后，将 Deepseek 的输出保存到 `StudyPlan/学习笔记/` 对应文件中

---

## T01_蓝牙整体架构理解

```
你是一位 Android 蓝牙 Framework 架构专家。我是一名车载 Android 开发工程师，正在学习 Android 蓝牙协议栈。

请详细讲解 Android 蓝牙协议栈的整体架构，要求：

1. 画出完整的分层架构图（从 App 层到 HCI/HAL 层），每层标注：
   - 层名和英文缩写
   - 使用的编程语言
   - 核心职责（1-2句话）

2. 详细解释层间通信机制：
   - 上层调用下层：用什么方式？（API函数/IPC/回调）
   - 下层通知上层：用什么方式？（Callback/Broadcast/事件）
   - 举例说明：A2DP connect 请求从 App 到 HCI 的完整调用链

3. 解释线程模型：
   - 蓝牙进程中有哪些关键线程？
   - 线程之间如何同步？
   - 为什么 BTIF 层的函数调用需要做线程切换？

4. 解释 GD 新架构与旧架构的关系：
   - 为什么要引入 GD？
   - 新旧架构如何共存？（Shim 适配层）
   - 最终目标是什么？

5. 针对车载开发场景，指出：
   - 车载开发最常修改的是哪几层？
   - 车载开发最常遇到的问题通常出在哪几层？

请用中文回答，技术术语保留英文。
```

---

## T02_蓝牙开关流程详解

```
你是一位 Android 蓝牙 Framework 架构专家。我是一名车载 Android 开发工程师。

请详细讲解 Android 蓝牙的开关流程，要求：

1. 蓝牙开启流程（BluetoothAdapter.enable()）：
   - 从 App 调用开始，逐层追踪到 HCI 命令发送
   - 列出每一层的关键函数名和文件路径
   - 画出时序图（App → Framework → Service → JNI → BTIF → BTA → GD → HCI）
   - 蓝牙芯片初始化过程中发送了哪些 HCI 命令？
   - 初始化完成后如何通知上层？

2. 蓝牙关闭流程（BluetoothAdapter.disable()）：
   - 关闭时各 Profile 的清理顺序是什么？
   - 如何确保所有连接都断开后再关闭？
   - 关闭过程中的超时处理？

3. 蓝牙状态机：
   - Adapter 的状态有哪些？（OFF → BLE_ON → ON → TURNING_ON → TURNING_OFF）
   - 状态转换条件和触发事件
   - 画出状态转换图

4. 车载特殊场景：
   - 车机休眠/唤醒时蓝牙的状态变化
   - Auto-On 功能的实现（AutoOn.kt）
   - 蓝牙进程崩溃后的自动恢复机制

5. 常见问题：
   - 蓝牙开关卡在 TURNING_ON/TURNING_OFF 的原因和定位方法
   - 蓝牙开启失败的可能原因
   - 如何从日志中判断开关流程卡在哪一步？

请用中文回答，技术术语保留英文。关键函数名请给出完整签名。
```

---

## T03_设备扫描流程详解

```
你是一位 Android 蓝牙 Framework 架构专家。我是一名车载 Android 开发工程师。

请详细讲解 Android 蓝牙的设备扫描流程，要求：

1. BR/EDR 扫描（startDiscovery）：
   - 从 App 调用到 HCI Inquiry 命令的完整调用链
   - Inquiry 的类型和参数（General/Limited, 时长等）
   - 扫描结果的上报流程（HCI Event → BTA → BTIF → JNI → Java）
   - 设备名称获取（Remote Name Request）的时机

2. BLE 扫描（startScan）：
   - 从 App 调用到 HCI LE_Set_Scan_Enable 的完整调用链
   - 扫描模式（Low Latency / Low Power / Balanced）
   - 扫描结果的上报流程
   - 扫描过滤（Filter）机制

3. 双模扫描：
   - BR/EDR 和 BLE 扫描能否同时进行？
   - Android 是如何处理双模扫描的？

4. 关键数据结构：
   - 扫描结果在每一层的表示形式
   - BluetoothDevice 对象的创建时机

5. 常见问题：
   - 扫描不到设备的可能原因
   - 扫描结果丢失/重复的处理
   - 扫描对已连接设备的影响

请用中文回答，技术术语保留英文。
```

---

## T04_配对流程详解

```
你是一位 Android 蓝牙 Framework 架构专家。我是一名车载 Android 开发工程师。

请详细讲解 Android 蓝牙的配对流程，要求：

1. SSP（Secure Simple Pairing）流程：
   - IO Capability Exchange 的完整过程
   - 四种配对模式：Just Works / Numeric Comparison / Passkey Entry / OOB
   - 每种模式的用户交互和安全性分析
   - 从 App 调用 createBond() 到配对完成的完整调用链

2. LE 配对流程：
   - LE Security Modes (Mode 1 Level 1-4)
   - LE Legacy Pairing vs LE Secure Connections
   - SMP (Security Manager Protocol) 的交互过程
   - 从 App 调用到配对完成的完整调用链

3. 配对信息存储：
   - Link Key 的存储位置和格式
   - LE LTK (Long Term Key) 的存储
   - 配对信息在蓝牙重启后的恢复

4. 取消配对（removeBond）：
   - 完整调用链
   - 如何确保所有 Profile 断开连接
   - 配对信息删除的时机

5. 车载特殊场景：
   - 车机无屏幕无输入设备的配对处理
   - 自动配对（Auto-Pair）的实现
   - 多用户场景下的配对管理
   - CarPlay/HiCar 的特殊配对流程

6. 常见问题：
   - 配对失败的常见原因和定位方法
   - 配对超时的处理
   - 兼容性问题（Interop 数据库的使用）

请用中文回答，技术术语保留英文。
```

---

## T05_Profile连接流程详解

```
你是一位 Android 蓝牙 Framework 架构专家。我是一名车载 Android 开发工程师。

请详细讲解 Android 蓝牙的 Profile 连接流程，要求：

1. Profile 连接通用流程：
   - BluetoothProfile.connect() 的通用调用链
   - Profile 代理对象（Proxy）的获取流程（BluetoothProfile.ServiceListener）
   - Profile 连接状态回调（onConnectionStateChanged）
   - Profile 连接的优先级管理（Priority: PRIORITY_ON/OFF/AUTO_CONNECT）

2. A2DP 连接流程：
   - 从 BluetoothA2dp.connect() 到 AVDT 建链的完整调用链
   - SDP 查询 → AVDT Discover → GetCap → SetConfig → Open
   - 编解码器协商的时机和流程
   - 连接超时处理

3. HFP 连接流程：
   - 从 BluetoothHeadset.connect() 到 SLC 建立的完整调用链
   - RFCOMM 连接 → AT 命令能力协商 → SLC 完成
   - WBS/SWB 编解码协商

4. 自动连接（Auto-Connect）：
   - 蓝牙开启后的自动连接机制
   - 自动连接的优先级顺序
   - 自动连接失败的重试机制
   - 车载场景的自动连接优化

5. 多设备连接：
   - 同时连接多部手机的实现
   - 主设备/副设备的概念
   - 音频焦点在多设备间的切换

6. 常见问题：
   - Profile 连接失败的常见原因
   - 连接卡在 CONNECTING 状态的定位方法
   - A2DP 连接成功但无音频的处理

请用中文回答，技术术语保留英文。
```

---

## T06_回调机制详解

```
你是一位 Android 蓝牙 Framework 架构专家。我是一名车载 Android 开发工程师。

请详细讲解 Android 蓝牙协议栈中的回调机制，要求：

1. HAL 回调机制：
   - bt_callbacks_t 结构体的组成和注册时机
   - 各 Profile 的回调结构体（btav_callbacks_t, bthf_callbacks_t 等）
   - 回调函数从 C++ 到 Java 的传递路径

2. JNI 回调实现：
   - callback_env 的获取和管理
   - CallVoidMethod 等 JNI 调用的线程安全
   - 回调从 BTIF 线程到 Java 线程的切换

3. BTA 事件机制：
   - bta_sys_sendmsg() 的工作原理
   - BTA 事件循环（bta_sys_main.cc）
   - 事件处理函数的注册和分发

4. GD 层回调：
   - base::Callback / base::Bind 的使用模式
   - GD 层与旧栈的回调桥接

5. 实际案例分析：
   - A2DP 连接状态变化：从 BTA 事件到 Java 回调的完整路径
   - HFP 来电通知：从 AT 命令到 Java 回调的完整路径
   - 配对状态变化：从 SMP 事件到 Java 回调的完整路径

请用中文回答，技术术语保留英文。关键代码请给出函数签名和文件路径。
```

---

## T07_蓝牙状态机详解

```
你是一位 Android 蓝牙 Framework 架构专家。我是一名车载 Android 开发工程师。

请详细讲解 Android 蓝牙协议栈中的状态机模式，要求：

1. 通用状态机框架：
   - common/state_machine.h 的实现原理
   - 状态机的创建、启动、停止
   - 状态转换的处理流程

2. A2DP 状态机（btif_av.cc）：
   - 有哪些状态？（Idle/Opening/Opened/Started/Closing）
   - 每个状态下处理哪些事件？
   - 状态转换图
   - Source 和 Sink 的状态机区别

3. HFP AG 状态机（bta_ag_act.cc）：
   - 有哪些状态？
   - AT 命令如何触发状态转换
   - SCO 状态与 HFP 状态的关系

4. BTA DM 状态机（bta_dm_act.cc）：
   - 设备管理的状态
   - 扫描/配对/连接的状态转换

5. AV Stream 状态机（bta_av_ssm.cc）：
   - 音频流的状态管理
   - 与 A2DP 状态机的协作

6. 状态机调试技巧：
   - 如何从日志中追踪状态转换
   - 状态机卡死的常见原因
   - 如何添加自定义的状态机日志

请用中文回答，技术术语保留英文。请画出关键状态机的状态转换图。
```
