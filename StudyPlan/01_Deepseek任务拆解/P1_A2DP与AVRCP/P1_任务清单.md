# P1_A2DP与AVRCP — Deepseek 任务拆解

> 执行顺序：T08 → T09 → ... → T12
> 前置依赖：P0 全部完成

---

## T08_A2DP连接流程源码追踪

```
你是一位 Android 蓝牙协议栈源码分析专家。我是一名车载 Android 开发工程师。

请基于 AOSP Android 16 Fluoride 蓝牙协议栈源码，详细追踪 A2DP 连接的完整流程。

源码根目录: packages/modules/Bluetooth/system/

要求：

1. 从 BluetoothA2dp.connect(device) 开始，逐层追踪，列出每一层的关键函数：
   - Framework 层: A2dpService.connect() 的实现
   - JNI 层: 对应的 JNI 函数
   - BTIF 层: btif_av.cc 中的连接入口函数
   - BTA 层: bta_av_act.cc 中的处理函数
   - Stack 层: AVDT 相关函数
   - HCI 层: 最终发送的 HCI 命令

2. 详细分析 btif_av.cc 中 A2DP 状态机的连接过程：
   - Idle → Opening 的触发条件和处理
   - Opening → Opened 的触发条件和处理
   - 连接过程中发送的 AVDT 信令消息序列

3. SDP 查询过程：
   - 何时发起 SDP 查询？
   - 查询哪些 UUID？
   - 查询结果如何影响后续连接？

4. AVDT 信令流程：
   - DISCOVER → GET_CAPABILITIES → SET_CONFIGURATION → OPEN → START
   - 每个信令消息的参数和含义
   - 信令失败的处理

5. 车载 A2DP Sink 角色的特殊处理：
   - 车机作为 Sink 接收音频的代码路径
   - Source 和 Sink 的状态机差异

请用中文回答，函数名和文件路径保留英文。画出完整的时序图。
```

---

## T09_A2DP编解码器协商详解

```
你是一位 Android 蓝牙协议栈源码分析专家。我是一名车载 Android 开发工程师。

请详细讲解 A2DP 编解码器协商的完整流程，要求：

1. 编解码器列表和优先级：
   - Android 支持的编解码器：SBC/AAC/aptX/aptX-HD/LDAC/LC3/Opus
   - 每种编解码器的参数（采样率/声道/比特率）
   - 优先级排序逻辑

2. 协商流程：
   - 本地能力上报
   - 远端能力获取（通过 AVDT GetCap）
   - 编解码器选择算法（btif_av_co.cc）
   - 参数配置（采样率、声道模式等）
   - 配置确认

3. A2DP Offload 模式下的编解码器协商：
   - Offload 编解码器如何注册？
   - Offload 模式下的协商流程差异
   - a2dp_offload.supported 属性的影响

4. 编解码器切换：
   - 运行时切换编解码器的流程
   - BluetoothCodecConfig 的使用
   - setCodecConfigPreference() 的实现

5. 车载特殊场景：
   - 车载 DSP 支持的编解码器如何注册
   - 如何强制使用特定编解码器
   - 编解码器协商失败的降级策略

6. 常见问题：
   - 协商结果不符合预期的定位方法
   - 编解码器参数不兼容的处理
   - Offload 模式下编解码器初始化失败

请用中文回答，技术术语保留英文。关键代码请给出函数签名和文件路径。
```

---

## T10_A2DP音频数据通路详解

```
你是一位 Android 蓝牙协议栈源码分析专家。我是一名车载 Android 开发工程师。

请详细讲解 A2DP 音频数据通路的实现，要求：

1. Software 编解码模式（车机作为 Sink）：
   - 音频数据从 HCI 接收到播放的完整路径
   - btif_a2dp_sink.cc 的数据处理流程
   - SBC/AAC 解码器的调用
   - AudioTrack 的使用

2. Offload 编解码模式：
   - 音频数据从 HCI 到 DSP 的路径
   - audio_hal_interface/a2dp_encoding.cc 的作用
   - Offload 数据通路的启动和停止
   - Offload 模式下的音频焦点管理

3. 音频会话管理：
   - A2DP 音频会话的创建和销毁
   - 音频焦点请求和释放
   - 与其他音频源（电话/导航）的优先级处理

4. 音频控制：
   - 播放/暂停的触发和通知
   - 音频挂起（Suspend）和恢复
   - 音频状态回调（AUDIO_STATE_STARTED/STOPPED/SUSPENDED）

5. 常见问题：
   - A2DP 连接成功但无音频
   - 音频卡顿/断续的原因和定位
   - Offload 模式下的音频异常
   - 音频延迟过高的优化

请用中文回答，技术术语保留英文。
```

---

## T11_AVRCP控制与元数据详解

```
你是一位 Android 蓝牙协议栈源码分析专家。我是一名车载 Android 开发工程师。

请详细讲解 AVRCP 的实现，要求：

1. AVRCP 角色和版本：
   - CT (Controller) vs TG (Target) 的区别
   - 车机通常同时实现 CT 和 TG
   - AVRCP 1.3/1.4/1.5/1.6 的功能差异

2. AVRCP 控制命令（CT端）：
   - Play/Pause/Stop/Next/Previous 的实现
   - 从 App 调用到 AVCT 命令发送的完整路径
   - 按键事件的传递（KEY_EVENT vs PASSTHROUGH）

3. AVRCP 元数据（Metadata）：
   - 歌曲信息获取（Now Playing Information）
   - 播放状态获取（Play Status）
   - 元数据变化的通知机制

4. AVRCP 浏览（Browsing）：
   - 文件夹浏览的实现
   - 播放列表管理
   - Browsing 的连接建立

5. Absolute Volume（车载关键）：
   - 绝对音量控制的工作原理
   - Volume Changed Notification 的注册和处理
   - SetAbsoluteVolume 命令的发送和响应
   - 车机音量与手机音量的同步机制

6. 封面艺术（Cover Art）：
   - AVRCP 1.5/1.6 的封面艺术实现
   - 通过 BIP 或 BLE 传输封面

7. 新旧实现对比：
   - btif_rc.cc（旧实现）
   - avrcp_service.cc（新实现, Android 13+）
   - 两者的差异和迁移

8. 常见问题：
   - 歌曲信息不更新的原因
   - 播放控制命令无响应
   - 音量不同步的定位方法

请用中文回答，技术术语保留英文。
```

---

## T12_A2DP_AVRCP问题定位实战

```
你是一位 Android 蓝牙协议栈问题定位专家。我是一名车载 Android 开发工程师。

请讲解 A2DP/AVRCP 相关问题的定位方法，要求：

1. 日志分析技巧：
   - A2DP 相关的日志 Tag 和关键日志行
   - AVRCP 相关的日志 Tag 和关键日志行
   - 如何从日志中提取连接状态变化
   - 如何从日志中提取编解码器协商结果

2. HCI Snoop Log 分析：
   - A2DP 连接过程的 Snoop 抓包分析
   - AVDT 信令消息的解读
   - 音频数据流的检查
   - AVRCP 命令/响应的解读

3. 典型问题案例：
   案例1: A2DP 连接成功但无音频
   - 可能原因列表（按概率排序）
   - 每种原因的验证方法
   - 修复方案

   案例2: 蓝牙音乐卡顿
   - 可能原因（干扰/带宽/编解码/Offload）
   - 定位步骤
   - 优化方案

   案例3: AVRCP 歌曲信息不显示
   - 可能原因
   - 定位方法
   - 修复方案

   案例4: 音量不同步
   - Absolute Volume 机制排查
   - 定位步骤

   案例5: A2DP Offload 模式异常
   - Offload 初始化检查
   - DSP 通信检查
   - 降级到 Software 模式的方法

4. dumpsys 输出解读：
   - adb dumpsys bluetooth_manager 中 A2DP/AVRCP 相关信息
   - 如何从中提取关键信息

请用中文回答，给出具体的日志行示例和命令。
```
