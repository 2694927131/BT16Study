# P3_手机互联 — Deepseek 任务拆解

> 执行顺序：T17 → T18 → T19
> 前置依赖：P0 完成，P1 的 T08/T11 有助于理解

---

## T17_BLE_GATT完整流程详解

```
你是一位 Android 蓝牙协议栈源码分析专家。我是一名车载 Android 开发工程师。

请详细讲解 BLE GATT 的完整实现，要求：

1. BLE 扫描：
   - BluetoothLeScanner.startScan() 的完整调用链
   - GD 层 Le Scanning Manager 的实现
   - 扫描参数配置（间隔/窗口/模式）
   - 扫描结果的上报路径

2. BLE 连接：
   - BluetoothGatt.connect() 的完整调用链
   - HCI: LE_Create_Connection → Connection Complete Event
   - 连接参数（间隔/延迟/超时）
   - 连接参数更新流程

3. GATT 服务发现：
   - discoverServices() 的完整调用链
   - ATT 协议的 Read By Type / Find Information 消息
   - Service/Characteristic/Descriptor 的解析
   - 缓存机制

4. GATT 读写操作：
   - writeCharacteristic() 的完整调用链
   - readCharacteristic() 的完整调用链
   - Write With Response vs Write Without Response
   - Prepared Write 和 Reliable Write

5. GATT 通知（Notification/Indication）：
   - 注册通知的流程（写 CCC Descriptor）
   - Notification 的接收路径
   - Indication 的确认机制

6. GATT Server（车机作为外设）：
   - BluetoothGattServer 的使用
   - 服务的添加和广播
   - 连接请求的处理
   - 数据读写的响应

7. 手机互联中的 GATT 应用：
   - HiCar 的 BLE 发现和握手
   - CarPlay 的 BLE 认证
   - 通用 BLE 握手→WiFi切换模式

请用中文回答，技术术语保留英文。
```

---

## T18_RFCOMM_SPP通道详解

```
你是一位 Android 蓝牙协议栈源码分析专家。我是一名车载 Android 开发工程师。

请详细讲解 RFCOMM/SPP 通道的实现，要求：

1. RFCOMM 协议基础：
   - RFCOMM 帧格式
   - DLCI (Data Link Connection Identifier)
   - 多路复用（一个L2CAP通道上多个RFCOMM连接）

2. RFCOMM 连接流程：
   - BluetoothSocket.connect() 的完整调用链
   - btif_sock_rfc.cc 的实现
   - BTA_JvRfcommStart() 的处理
   - L2CAP 连接 → RFCOMM SABM → PN → MSC → 数据通道

3. SDP 查询：
   - 查询 SPP Service Record
   - 获取 RFCOMM Channel Number
   - UUID 匹配

4. SPP 数据传输：
   - InputStream/OutputStream 的实现
   - 数据帧的收发
   - 流控机制
   - 大数据传输的优化

5. RFCOMM Server（车机监听）：
   - BluetoothServerSocket 的使用
   - SDP 服务注册
   - 连接请求的接受

6. 手机互联中的 RFCOMM 应用：
   - HiCar 的数据通道
   - CarLink 的通信通道
   - OBD 诊断仪的通信

7. 常见问题：
   - RFCOMM 连接失败
   - 数据传输中断
   - 端口占用冲突

请用中文回答，技术术语保留英文。
```

---

## T19_手机互联蓝牙问题定位

```
你是一位 Android 蓝牙协议栈问题定位专家。我是一名车载 Android 开发工程师。

请讲解手机互联（HiCar/CarPlay/CarLink）蓝牙相关问题的定位方法，要求：

1. 互联蓝牙流程的通用模式：
   - BLE 发现 → GATT 握手 → WiFi 参数交换 → WiFi 连接
   - 每一步的关键日志 Tag
   - 每一步的 Snoop Log 特征

2. BLE 发现阶段问题：
   - 扫描不到互联设备
   - 特定 UUID 的广播过滤
   - 扫描结果解析异常

3. GATT 握手阶段问题：
   - GATT 连接失败
   - Service Discovery 超时
   - Characteristic 读写失败
   - 认证数据交换异常

4. WiFi 切换阶段问题：
   - WiFi 参数获取失败
   - WiFi 连接建立失败
   - 蓝牙与WiFi的协作问题

5. 典型问题案例：

   案例1: HiCar 连接失败
   - 排查步骤
   - 关键日志和 Snoop 分析

   案例2: CarPlay 认证失败
   - 排查步骤
   - Apple 认证协议的关键点

   案例3: 互联频繁断连
   - 蓝牙连接稳定性检查
   - WiFi 信号质量检查
   - 心跳机制检查

6. 互联兼容性：
   - 不同手机品牌的互联差异
   - iOS vs Android 的互联流程差异
   - 兼容性问题的定位方法

请用中文回答，给出具体的排查步骤和命令。
```
