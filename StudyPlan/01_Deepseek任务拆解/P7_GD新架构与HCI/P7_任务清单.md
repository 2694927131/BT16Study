# P7_GD新架构与HCI — Deepseek 任务拆解

> 执行顺序：T30 → T31 → T32
> 前置依赖：P0 完成

---

## T30_GD架构与Shim适配层详解

```
你是一位 Android 蓝牙协议栈架构专家。我是一名车载 Android 开发工程师。

请详细讲解 GD (Gable Dodge) 新架构的实现，要求：

1. GD 架构设计理念：
   - 为什么要引入 GD？
   - GD 与旧架构的核心设计差异
   - GD 的模块化设计
   - GD 的可测试性设计

2. GD 核心模块：
   - gd/hci/: 新 HCI 层实现
   - gd/hal/: HAL 抽象层
   - gd/os/: 操作系统抽象（线程/定时器/队列/Reactor）
   - gd/packet/: 数据包框架
   - gd/storage/: 设备存储
   - gd/crypto_toolbox/: 加密工具
   - gd/common/: 通用工具类

3. GD OS 抽象层：
   - gd/os/thread.h: 线程抽象
   - gd/os/handler.h: 事件处理器
   - gd/os/queue.h: 线程安全队列
   - gd/os/alarm.h: 定时器
   - gd/os/reactor.h: Reactor 模式
   - 这些抽象如何保证可移植性？

4. GD Packet 框架：
   - packet_builder.h / packet_view.h 的设计
   - 类型安全的数据包构建和解析
   - 零拷贝设计
   - 自动代码生成（packet/parser/）

5. Shim 适配层：
   - main/shim/ 的作用
   - 新旧 API 的映射
   - 逐步迁移策略
   - 当前迁移进度

6. 对车载开发的影响：
   - 哪些模块已经迁移到 GD？
   - 哪些还在旧架构？
   - 车载开发需要注意的兼容性问题

请用中文回答，技术术语保留英文。
```

---

## T31_GD_HCI层与HAL层详解

```
你是一位 Android 蓝牙协议栈源码分析专家。我是一名车载 Android 开发工程师。

请详细讲解 GD HCI 层和 HAL 层的实现，要求：

1. GD HCI 层架构：
   - hci_layer.cc: HCI 层核心
   - controller.cc: 控制器接口
   - acl_manager/: ACL 连接管理
   - le_address_manager.cc: LE 地址管理（隐私）
   - le_scanning_manager/: LE 扫描
   - le_advertising_manager/: LE 广播
   - security_module/: 安全模块

2. HCI 命令/事件处理：
   - HCI 命令的发送流程
   - HCI 事件的接收和分发
   - Command Complete / Command Status 事件
   - HCI 数据路由

3. HAL 层实现：
   - hci_hal.h: HAL 抽象接口
   - hci_backend_aidl.cc: AIDL HAL 后端
   - hci_backend_hidl.cc: HIDL HAL 后端
   - AIDL vs HIDL 的差异和迁移

4. Snoop Logger：
   - snoop_logger.cc 的实现
   - Snoop Log 的写入时机
   - 过滤模式（filtered mode）
   - 隐私保护（地址混淆）

5. 芯片通信：
   - HCI 命令如何到达蓝牙芯片
   - UART / USB / SDIO 传输方式
   - 芯片初始化序列

请用中文回答，技术术语保留英文。
```

---

## T32_GD_Storage与Crypto详解

```
你是一位 Android 蓝牙协议栈源码分析专家。我是一名车载 Android 开发工程师。

请详细讲解 GD Storage 和 Crypto 模块的实现，要求：

1. GD Storage 模块：
   - storage_module.h: 存储模块接口
   - config_cache.cc: 配置缓存
   - device.cc / classic_device.h / le_device.cc: 设备信息存储
   - mutation.cc: 事务性修改
   - 存储的持久化机制

2. GD Crypto Toolbox：
   - aes.cc: AES 加密实现
   - 蓝牙安全相关的加密算法
   - SMP 配对中的加密操作

3. GD Common 工具：
   - bidi_queue.h: 双向队列
   - blocking_queue.h: 阻塞队列
   - circular_buffer.h: 环形缓冲区
   - lru_cache.h: LRU 缓存
   - callback.h: 回调封装
   - stop_watch.h: 性能计时

4. GD Metrics：
   - metrics.h: 度量统计接口
   - 蓝牙性能数据的收集和上报

请用中文回答，技术术语保留英文。
```
